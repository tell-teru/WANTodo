<!DOCTYPE html>
<html lang="ja">
    
    <body>
        <!--<img src="/images/signup.jpg">-->
        <div>
            <div>
                <p><b>Sign in</b></p>
            </div>
            
            <div align="center" class="container">
                <form action="/signin" method="post">
                    <input class="form-control" name="user" placeholder="user name"><br>
                    <input class="form-control" name="password" type="password" placeholder="password"><br>
                    <div class="opacity-50">
                        <input class="form-control btn btn-dark mb-3" type="submit" value="SIGN IN">
                    </div>
                </form>
            </div>
            
            <ul class="container">
                <% if current_user.nil? %>
                  <a href="/signup" class="nav-link"><p>新規登録はこちら</p></a>
                <% else %>
                    <a href="/" class="nav-link">
                        <p><%= current_user.name %> としてログイン済み</p>
                    </a>
                <% end %>
            </ul>
        </div>
    </body>
</html>
