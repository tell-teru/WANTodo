<!DOCTYPE html>
<html lang="ja">
	<head>
		<meta charset="UTF-8">
		<title><%= @title %></title>
		<!--エラー出る-->
		<!--<link rel="stylesheet" href="/assets/css/style.css">-->
    <link href="/css/style.css" rel="stylesheet">
		<link href="https://fonts.googleapis.com/css?family=Righteous" rel="stylesheet">
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
	  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
  </head>
  <style>
    :root {
      --main-color: #ECD53E;
      --text-color: #1F1F1F;
      --border-color: #F0F3F4;
      --hover-color: #F3F3F3;
    }
    
    header {
      position: fixed;
      z-index: 999;
      top: 0;
      left: 0;
      width: 100%;
      padding: 10px 40px;
      background: #eee;
      box-sizing: border-box;
      transition: padding .3s;
    }
    header .inner {
      position: relative;
      display: flex;
      align-items: center;
      height: 100%;
    }
    .logo {
      position: relative;
      margin: 0;
      padding: 0;
      font-weight: bold;
    }
    
    
    /* header-navi */
    
    header .navi {
      margin: 0 0 0 auto;
      padding: 0;
      font-size: 14px;
      font-weight: bold;
      display: flex;
      list-style: none;
    }
    header .navi li {
      margin: 5px 0 5px 40px;
    }
    header .navi li:first-child {
      margin-left: 0;
    }
    header .navi li a {
      display: block;
      box-sizing: border-box;
      text-decoration: none;
      color: #333;
    }
    /*ホバー時に下線がつく*/
    /*header .navi li a:hover {*/
    /*  text-decoration: underline;*/
    /*}*/
    
    /* add css  */
    
    #header.scroll {
      padding: 10px 40px;
    }

    
    body, input {
      background-color: lightyellow;/* 背景色 */
      color: var(--text-color);
    }
    
    .container {
      display: flex;
      max-width: 1080px;
      margin: 0 auto;
      margin-top: 60px;
      border-right: solid 1px var(--border-color);
    }
    
    .contents {
      margin: 20px 25px 15px 25px;
    }
    
    main {
      width: 100%;
    }
    
    span, p, h1, h2, label {
      color: var(--text-color);
    }
    
    h1 {
      font-size: 20px;
    }
    
    .button {
      display: block !important;
      height: 50px;
      padding: 12px 0 10px 0 !important;
      font-size: 17px;
      color: #ffffff;
      background-color: var(--main-color);
      font-weight: 700;
      text-align: center;
    }
    
    .button:hover {
      background-color: var(--main-color) !important;
      opacity: 0.75 !important;
    }
    
    .icon {
      width: 45px;
      height: 45px;
      border-radius: 30px;
      object-fit: cover;
    }
    .genre_icon {
      width: 45px;
      height: 45px;
      /*border-radius: 30px;*/
      /*object-fit: cover;*/
    }
    
    .header {
      border-bottom: solid 1px var(--border-color);
    }
    
    a {
      text-decoration: none;
    }
    
    a:hover {
      opacity: 0.5;
    }
    
    nav {
      width: 330px;
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      padding: 0 20px 0 20px;
      border-right: solid 1px var(--border-color);
    }
    
    nav .icon.tobisuke {
      margin-top: 20px;
      width: 50px;
      height: 50px;
      /*margin-left: 20px;*/
    }
    
    nav .icon {
      width: 30px;
      height: 30px;
    }
    
    nav .top {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    nav .top a {
      height: 50px;
      padding: 10px 10px 10px 10px;
      border-radius: 25px;
      display: flex;
      gap: 10px;
      align-items: center;
    }
    
    nav .top a.active {
      /*選択中の背景色*/
      /*background-color: var(--hover-color);*/
      background-color: royalblue;
      color: var(--text-color); /* 選択中の文字色 */
    }
    
    nav .top a:hover {
      background-color: var(--hover-color);
      opacity: 1;
    }
    
    nav .top a img {
      width: 20px;
      height: 20px;
    }
    
    nav .top a span {
      font-size: 20px;
      font-weight: bold;
    }
    
    nav .bottom{
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 20px;
    }
    
    nav .bottom .user-profile {
      display: flex;
      align-items: center;
      gap: 10px;
    }
    
    nav .bottom .user-profile span {
      font-weight: bold;
      font-size: 18px;
    }
    
    nav .bottom a .logout-icon {
      width: 20px;
      height: 20px;
    }
    
    /* index, comments */
    .post {
      border-top: solid 1px var(--border-color);
      /*background-color: wheat;  */
      background-color: #EEDEC9;
      border-radius: 50px; /* 角丸にする半径を指定 */
      margin: 30px 0; /* 上下のマージンを設定 */
      padding: 5px; /* コンテンツの内側の余白を設定 */
    }
    
    .post .text {
      width: 100%;
    }
    
    .post .contents {
      display: flex;
      gap: 10px;
    }
    
    .post .contents .title .user-name {
      font-size: 30px;
      font-weight: bold;
    }
    
    .post .actions {
      width: 100%;
      margin-top: 0px;
      display: flex;
      justify-content: space-between;
      justify-content: flex-end;
    }
    
    .post .actions input[type="submit"], .post .actions a {
      height: auto;
      color: var(--text-color);
      background: none;
      font-weight: 500;
    }
    
    .comment .post {
      border-bottom: solid 1px var(--border-color);
    }
    
    
    /* profile */
    .profile {
      position: relative;
    }
    
    .profile .header-photo {
      background-color: #D9D9D9;
      height: 150px;
    }
    
    .profile{
      margin: -60px 0 20px 0;
      width: 120px;
      height: 120px;
      border-radius: 60px;
    }
    
    .profile .edit-profile-button {
      position: absolute;
      top: 170px;
      right: 20px;
      border: 1px solid var(--text-color);
      padding: 8px 20px;
      border-radius: 100px;
      color: var(--text-color);
      font-weight: bold;
    }
    
    /* form */
    form {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }
    
    form div {
      display: flex;
      flex-direction: column;
      gap: 5px;
    }
    
    form div label {
      font-size: 18px;
      font-weight: bold;
      background-color: lightyellow;
    }
    
    form div input[type=text], form div input[type=password], form div textarea, form div select {
      min-height: 50px;
      padding: 0 15px;
      border-radius: 30px;
      border: solid 1.5px var(--border-color);
      background-color: #FFFFFF;
    }
    
    form div textarea {
      min-height: 150px;
      padding: 15px;
    }
    
    form div textarea[class="comment"] {
      min-height: 100px;
    } 
  
    .user-icon {
      width: 45px;
      height: 45px;
      border-radius: 30px;
      object-fit: cover;
    }
  
    .button {
      display       : inline-block;
      border-radius : 13%;          /* 角丸       */
      font-size     : 18pt;        /* 文字サイズ */
      text-align    : center;      /* 文字位置   */
      cursor        : pointer;     /* カーソル   */
      padding       : 12px 12px;   /* 余白       */
      background    : #003366;     /* 背景色     */
      color         : #ffffff;     /* 文字色     */
      line-height   : 1em;         /* 1行の高さ  */
      transition    : .3s;         /* なめらか変化 */
      box-shadow    : 1px 1px 3px #666666;  /* 影の設定 */
      border        : 2px solid #003366;    /* 枠の指定 */
    }
    .button:hover {
      box-shadow    : none;        /* カーソル時の影消去 */
      color         : #003366;     /* 背景色     */
      background    : #ffffff;     /* 文字色     */
    }
    
    /*チェックボックス*/
    
    .radioArea input[type="checkbox"] {
      width         : 22pt;        /* 大きさ：横 */
      height        : 22pt;        /* 大きさ：縦 */
      /*vertical-align: middle*/
      position:relative;
    }
    
    
    /*期間の記入部分の幅設定*/
    .date-range {
      width: 30%;
    }
    
    /*期間の記入部分の等間隔設定*/
    ul {
      display: -webkit-flex;
      display: flex;
      -webkit-justify-content: space-around;
      justify-content: space-around;
    }　
    
    .date-container {
      display: flex;
      justify-content: space-between; /* 要素を均等に配置 */
      align-items: center; /* 要素を垂直方向に中央揃えに配置 */
    }
    
    .parallel-even-distribution {
        display: flex;
        justify-content: space-between; /* ボタンを横並びに配置 */
    }
    
    
    .done_button {
      display       : inline-block;
      border-radius : 20%;          /* 角丸  */    
      font-size     : 18pt;        /* 文字サイズ*/ 
      text-align    : center;      /* 文字位置   */
      cursor        : pointer;     /* カーソル   */
      padding       : 12px 12px;   /* 余白       */
      background    : rgba(203, 203, 203, 0.75);     /* 背景色   */  
      color         : #7a7a7a;     /* 文字色     */
      line-height   : 1em;         /* 1行の高さ  */
      transition    : .3s;         /* なめらか変化 */
      box-shadow    : 1px 1px 3px #666666;  /* 影の設定 */
      border        : 2px solid rgba(203, 203, 203, 0.75);    /* 枠の指定 */
    }
    .done_button:hover {
      box-shadow    : none;        /* カーソル時の影消去 */
      color         : rgba(203, 203, 203, 0.75);     /* 背景色    */ 
      background    : #7a7a7a;     /* 文字色     */
    }
    


    
  </style>
  
	<body>
	  <% if !current_user.nil? %>
	  
  	  <header id="header" style="background-color: lightyellow;">
      <div class="navbar navbar-expand-lg navbar-light" style="background-color: white; border-radius: 30px;">
        <div class="container-fluid">
        <a class="navbar-brand" href="/" style="margin-left: 30px;">WANTodo</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          
          <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav me-auto mb-2 mb-md-0">
              <!-- ナビゲーションメニューの項目を左寄せに配置 -->
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/want/new"> +WANTodo </a>
              </li>
            </ul>
          </div>
      
          <!-- SIGN OUT を右寄せに配置 -->
          <div class="ml-auto" style="margin-right: 30px;">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/user/<%= current_user.id %>" style="color: black;"> <%= current_user.name %>のページ</a>
              </li>
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/signout" style="color: black;">SIGN OUT</a>
              </li>
            </ul>
          </div>
        </div>
      </div>

      
      </header>
      
      
      
    <% end %>
	  
		<div class="container">
      <nav>
        <div class="top">
          <br>
          <a href="/" class="<%= 'active' if request.path == '/' %>">
            <!--ホームに戻る-->
            <span> WANTodo </span>
          </a>
          
          <% if !current_user.nil? %>
          	<a href="/user/<%= current_user.id %>" class="<%= 'active' if request.path.start_with?('/user/') %>">
	            <img src=<%= current_user.img %> alt="ユーザーアイコン" class="icon">
	            <span><%= current_user.name %>のページ</span>
	          </a>
  	         
	          <!--メインページの時には所属グループ一覧-->
	          
  	          <!--# current_userが所属するグループのgroup_idを取得 -->
              <% part_groups = Part.where(user_id: current_user.id).pluck(:group_id) %>
               <!--# Groupテーブルからgroup_idが一致するレコードを取得-->
              <% matching_groups = Group.where(id: part_groups) %>
  	          
  	          <% if matching_groups.present? %>
                <% matching_groups.each do |group| %>
                  <a href="/group/page/<%= group.id %>">
                    <span><%= group.name %></span>
                  </a>
                <% end %>
              <% end %>
	          

	        <% else %>
	        	<a href="/signup" class="<%= 'active' if request.path == '/signup' %>">
	            <span> 新規登録 </span>
	          </a>
	          <a href="/signin" class="<%= 'active' if request.path == '/signin' %>">
	            <span> ログイン </span>
	          </a>
         	<% end %>
        </div>
          
      </nav>
    
		<main>
		  <br>
			<!--全部のerbファイルの中身-->
			<%= yield %>
		</main>
		</div>
	</body>
</html>